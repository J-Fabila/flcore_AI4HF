version: "1.0"

services:
  federado:
    container_name: server
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8888:8888
    volumes:
      - ./data:/workspace/data
    command: python main.py configs/client_1.yaml