# Device variables
device : cuda
n_gpu : 1
n_gpu_nodes : 1
num_workers : 0
client_id : 1
num_clients : 10
set_server : False
server_address : 37.222.187.198:8000

# Model variables
federated : True
use_certificates : False
local_model : MLP
features : maggic
configuration_file : configs/sample_config.json

#Resnet152
load_checkpoint : None
task : Classification
num_labels : 1
feature_size : 13
mlp_hidden_sizes : [128, 256, 128]
mlp_output_size : 64
ode_hidden_size : 64
ode_num_layers : 2
ode_batch_norm : False
time_nums: 50
MLP_preprocess : None

in_channels : 1
n_classes : 2

# Training variables
batch_size : 512
train_size : 0.8
val_size : 0.1
test_size : 0.1
epochs : 1
num_rounds : 10
verbatim : False

epochs_per_round : [5,10,15,20]
# Meaning 5 epochs in round 0,10 in round 1, ...

# Optimizer variables
optimizer : Adam # 'Adam' or 'SGD'
dropout : 0.1
lr : 0.001
lr_min : 1e-7
lr_factor : 0.5
lr_patience : 10
lr_scheduler : ReduceLROnPlateau
clip : 1.0
early_stopping_patience : 10

# Data set variables
data_folder : data/
n_channels : 1
data_path : data/
data_file : data.parquet

# Bias mitigation
bias_preprocessing : True
method : SUP
sensitive: ["patient_demographics_gender", "patient_demographics_age"]
# Either option works:
# sensitive: patient_demographics_gender
# sensitive: ["patient_demographics_gender", "patient_demographics_age"]

label: conditions_ap_any

# Logging variables
log_path : mlp_test_1
every_n_epochs : 1
wandb_track : False
wandb_project : MLP_Fed
# 60b70f9c192ed76eccd2a580effc8008936f6e78
wandb_run_name : Client_1
wandb_entity': 'jorgefabila1997'
save_top_k : 10
